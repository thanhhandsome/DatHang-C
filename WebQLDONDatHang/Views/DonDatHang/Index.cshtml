@model IEnumerable<WebQLDONDatHang.Models.hoadon>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/_LayoutPage1.cshtml";
}

<h2>Danh sách đơn hàng</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ngaylaphd)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tenkh)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ngaylaphd)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tenkh)
        </td>
        <td>
           
            @Html.ActionLink("Details", "XemChiTietDonDatHang","DonDatHang", new { id=item.sohd },new {@class="btn btn-danger" }) 
        
        </td>
    </tr>
}

</table>
